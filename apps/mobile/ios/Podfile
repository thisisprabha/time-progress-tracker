# Minimal Podfile for Native iOS App
# No Expo or React Native dependencies needed

platform :ios, '15.1'

target 'TimeProgressTracker' do
  # No pods needed - app uses only native SwiftUI, WidgetKit, UIKit
  # If you need any Swift packages in the future, add them here
end

target 'TimeLeftTrackerWidgetExtension' do
  # Widget extension also uses only native frameworks
end
